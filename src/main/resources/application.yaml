spring:
  application:
    name: springclouddemo
  output:
    ansi:
      enabled: always
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
